# Development Dockerfile to run Vite dev server inside container
FROM node:20-alpine

WORKDIR /app
ENV PATH /app/node_modules/.bin:$PATH

COPY package*.json ./
RUN npm ci

COPY . .

EXPOSE 5173

# Listen on all interfaces so host can connect
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
